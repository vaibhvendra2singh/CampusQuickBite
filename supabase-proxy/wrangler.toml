name = "supabase-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[build]
command = ""

# Uncomment below to use a custom domain after deploying (optional)
# [[routes]]
# pattern = "your-custom-domain.com/*"
# zone_name = "your-custom-domain.com"
